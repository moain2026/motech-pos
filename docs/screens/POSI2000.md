# POSI2000 — توثيق شاشة Onyx Pro POS

> النوع: **POSI** — إدخال/بيانات أساسية (Input/Master)  
> الحجم: 412,740 bytes · فك witchi: ✅ كامل · المصدر: `D:\YS_ERP\Forms\POSI2000.fmx`

## 1. الوظيفة

_(لا يوجد ملف مساعدة مطابق؛ الوظيفة مُستنتَجة من النوع والـ SQL)_

شاشة من فئة **إدخال/بيانات أساسية (Input/Master)**. أكثر الجداول استخداماً: `IAS_ITM_MST`, `PRIVILEGE`, `IAS_POS_CUSTOMER_CARD_AMOUNT`, `S_BRN`, `IAS_PRIV_CUSTOMER`, `IAS_POS_PAY_BILLS`.

## 2. Data Blocks (3)

| Block | نوع | QIU | Lock | ملاحظات |
| --- | --- | --- | --- | --- |
| IAS_POS_CUSTOMER_CARD_AMOUNT | DB | Q-- | AUTOMATIC |  |
| CTRL | ctrl | QIU | AUTOMATIC |  |
| PRNT | ctrl | QIU | AUTOMATIC |  |

## 3. Canvases (4) — التخطيط البصري

| Canvas | النوع | الأبعاد (W×H) | الظهور |
| --- | --- | --- | --- |
| CANVAS2 | CONTENT | 699×471 | visible |
| CANVAS28 | STACKED | 799×381 | visible |
| CANVAS75 | H_TOOLBAR | ?×20 | visible |
| PRNT | CONTENT | 234×168 | visible |


_ملاحظة: أبعاد الـ canvas بوحدة الـ character cell (8×16 px تقريباً للـ Forms 6i)._

## 4. Windows (2)

| Window | العنوان | النمط | Modal |
| --- | --- | --- | --- |
| WINDOW1 | أمــــــر الــتســــــليـــــم | DOCUMENT | false |
| PRNT |  | DIALOG | false |

## 5. Triggers (19)

**قياسية (Forms events):** `KEY-COMMIT`, `KEY-DELREC`, `KEY-EXIT`, `KEY-MENU`, `KEY-PRINT`, `ON-ERROR`, `ON-MESSAGE`, `PRE-FORM`, `WHEN-NEW-FORM-INSTANCE`, `WHEN-WINDOW-CLOSED`


**مخصّصة (custom):** `IS_DEL_REC`, `IS_UPDATE`, `LOCK_BLK`, `LOCK_TRGR`, `SHOW_DATA`, `UNDU_TRGR`, `UNLOCK_BLK`, `UNLOCK_BLK_UP`, `UPDT_TRGR`

## 6. Program Units (23)

**Packages (spec):** `FETCH_PKG`, `CHECK_DUPLICATE`, `LOCK_UNLOCK_ITEMS`


**Packages (body):** `FETCH_PKG`, `CHECK_DUPLICATE`


**Procedures/Functions (18):** `CUST_NAME`, `C_CURR`, `GET_I_SIZE`, `GET_PRIV`, `I_PRICE`, `POP_LST`, `SET_FRM_PRIV`, `SET_PARA`, `ITM_ALLOW_DIS`, `U_NAME`, `GET_ITM_FREEQTYPER`, `GET_ITM_WGHT`, `GET_ITM_VAT_PER`, `GET_ITM_NAME`, `CHK_MOV`, `SET_UNUSED`, `GET_ITM_DIS_LIMIT`, `CHCK_WT_ITM`

## 7. مكتبات مرفقة (1)

`D2KWUTIL`

## 8. Alerts (4)

| Alert | العنوان | النمط | زر1 |
| --- | --- | --- | --- |
| ADD_FROM |  | NOTE | نعم |
| DELETE_ALERT |  | WARNING | نعم |
| PRINT_ALERT | طباعة | NOTE | الشاشة |
| UNDO_ALERT |  | WARNING | نعم |

## 9. جداول قاعدة البيانات المرتبطة (من تحليل SQL في الـ .fmx)

| الجدول/الـ View | عدد مرات الظهور في SQL |
| --- | --- |
| IAS_ITM_MST | 9 |
| PRIVILEGE | 7 |
| IAS_POS_CUSTOMER_CARD_AMOUNT | 6 |
| S_BRN | 4 |
| IAS_PRIV_CUSTOMER | 3 |
| IAS_POS_PAY_BILLS | 2 |
| CUSTOMER | 2 |
| CUSTOMER_CURR | 2 |
| EX_RATE | 2 |
| IAS_PARA_GEN | 1 |
| IAS_PARA_INV | 1 |
| IAS_ITM_DTL | 1 |
| IAS_ITEM_PRICE | 1 |
| USER_R | 1 |
| IAS_CASH_IN_HAND_DTL | 1 |
| PRIV_CASH | 1 |
| PRIV_ACC | 1 |
| COST_CENTERS | 1 |
| COST_CENTER_TYPES | 1 |
| PRIVILEGE_CC | 1 |


**حِزَم DB المُستدعاة (PL/SQL packages):** `IAS_GEN_PKG`, `IAS_USR_PKG`, `FETCH_PKG`

## 10. عيّنة جُمل SQL مُستخرَجة (proof)
```sql
SELECT DECODE(:b1,1,'' '',''The field protected against update .'') FROM DUAL
SELECT DECODE(:b1,1,'' '',''This record exists .'') FROM DUAL
SELECT NVL(CC_AVAIL,0),NVL(USE_VAT,0) FROM IAS_PARA_GEN WHERE ROWNUM
SELECT NVL(USE_EXPIRE_DATE,0),NVL(USE_BATCH_NO,0),NVL(PRICE_TYPE,2),NVL(WTAVG_TYPE,1),NVL(COSTING_TYPE,2),NVL(ITEM_POSTING_FLAG,1),NVL(USE_WT_SYS,0) FROM IAS_PARA_INV WHERE ROWNUM
SELECT NVL(CARD_NO,0) FROM IAS_POS_PAY_BILLS WHERE CARD_NO
SELECT C_A_NAME FROM CUSTOMER WHERE C_CODE
SELECT CUR_NO,A_CY FROM CUSTOMER_CURR,EX_RATE WHERE CUSTOMER_CURR
SELECT 1 FROM IAS_PRIV_CUSTOMER WHERE U_ID = :b2 AND C_CODE = :b1 AND NVL(ADD_FLAG,0) = 1 AND A_CY = CUSTOMER_CURR.A_CY ) AND ROWNUM <= 1" GET_I_SIZE IAS_ITM_DTL YSPOS3 IAS_ITM_DTL IAS20193 IAS_ITM_DTL STANDARD "PKG INIT"GET_I_SIZE"P_ICODE"P_ITM_UNT"V_SIZE"<cursor ptr>"<SQL statement. Line 4>"" GET_I_SIZE SELECT P_SIZE FROM IAS_ITM_DTL WHERE I_CODE
SELECT NVL(AD_FLAG,0) FROM PRIVILEGE WHERE FORM_NO
SELECT NVL(MOD_FLAG,0) FROM PRIVILEGE WHERE FORM_NO
SELECT NVL(DEL_FLAG,0) FROM PRIVILEGE WHERE FORM_NO
SELECT NVL(VIEW_FLAG,0) FROM PRIVILEGE WHERE FORM_NO
```

## 11. ملاحظات / قيود

- منطق PL/SQL داخل الـ triggers/program-units = p-code (لا يُستعاد نصّه؛ الأسماء فقط). المنطق الكامل يُستخرَج من حِزَم DB عبر `ALL_SOURCE`.
- الـ items (الحقول داخل البلوكات) لا تُطبع في CLI الحالي — تُستكمل عبر Visual RE (screenshots) للـ layout والتسميات العربية.
- عدد الجُمل/الأسطر في ملف strings: راجع `_raw/POSI2000_strings.txt`.
