# Motech POS Frontend — environment template
# API base URL. In dev, leave as /api/v1 to use the Vite proxy → backend :3100.
# In production set to the deployed backend, e.g. https://api.example.com/api/v1
VITE_API_BASE_URL=/api/v1
